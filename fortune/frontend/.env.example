# API base URL for frontend fetch.
# Keep empty in local dev when using Vite proxy (/api -> localhost:5000).
# Example for static/preview deployment:
# VITE_API_BASE_URL=http://localhost:5000
VITE_API_BASE_URL=
